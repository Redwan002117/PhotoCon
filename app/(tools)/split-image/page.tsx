import SplitImageClient from '@/components/tools/SplitImageClient';

export const metadata = {
    title: 'Split Image Online (Grid Maker for Instagram) - PhotoCon',
    description: 'Split your photos into a grid (e.g., 3x3, 2x2). Great for Instagram grids, puzzles, and collages. Free and private.',
};

export default function SplitImagePage() {
    return <SplitImageClient />;
}
